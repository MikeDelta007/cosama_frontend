import { TypePiecePipe } from './type-piece.pipe';

describe('TypePiecePipe', () => {
  it('create an instance', () => {
    const pipe = new TypePiecePipe();
    expect(pipe).toBeTruthy();
  });
});

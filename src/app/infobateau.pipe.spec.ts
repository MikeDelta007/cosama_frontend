import { InfobateauPipe } from './infobateau.pipe';

describe('InfobateauPipe', () => {
  it('create an instance', () => {
    const pipe = new InfobateauPipe();
    expect(pipe).toBeTruthy();
  });
});

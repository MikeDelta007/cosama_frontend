import { Infovoyage3Pipe } from './infovoyage3.pipe';

describe('Infovoyage3Pipe', () => {
  it('create an instance', () => {
    const pipe = new Infovoyage3Pipe();
    expect(pipe).toBeTruthy();
  });
});

import { Infovoyage4Pipe } from './infovoyage4.pipe';

describe('Infovoyage4Pipe', () => {
  it('create an instance', () => {
    const pipe = new Infovoyage4Pipe();
    expect(pipe).toBeTruthy();
  });
});

import { InfoPlacePipe } from './info-place.pipe';

describe('InfoPlacePipe', () => {
  it('create an instance', () => {
    const pipe = new InfoPlacePipe();
    expect(pipe).toBeTruthy();
  });
});

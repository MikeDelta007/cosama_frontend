export class CltModeRglmt {
    constructor(
        public modergltId : number,                   // Identifiant du client
        public libelleModeReglm : string,                // Raison sociale
        public dureeJr : number,    // Prénom du contact (optionnel)
        public actif : boolean,     // Nom de famille du contact (optionnel)
) {}
}

import { Infovoyage2Pipe } from './infovoyage2.pipe';

describe('Infovoyage2Pipe', () => {
  it('create an instance', () => {
    const pipe = new Infovoyage2Pipe();
    expect(pipe).toBeTruthy();
  });
});

import { Infovoyage5Pipe } from './infovoyage5.pipe';

describe('Infovoyage5Pipe', () => {
  it('create an instance', () => {
    const pipe = new Infovoyage5Pipe();
    expect(pipe).toBeTruthy();
  });
});
